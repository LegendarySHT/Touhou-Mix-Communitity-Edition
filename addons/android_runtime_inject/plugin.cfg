[plugin]

name="AndroidRuntimeInject"
description="Export plugin: injects the AndroidRuntime plugin meta-data into the exported AndroidManifest so Engine.get_singleton(\"AndroidRuntime\") is available on Android."
author="THMIX"
version="1.0.0"
script="plugin.gd"
